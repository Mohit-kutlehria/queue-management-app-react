# Queue Management App

A lightweight and responsive queue management system built with React. It allows businesses or support centers to add customers to a queue, update their service status (waiting → serving → completed), and remove them when done.

## Features

- Add new customers with selected service type
- Update customer status (Waiting / Serving / Completed)
- Remove customers from the queue
